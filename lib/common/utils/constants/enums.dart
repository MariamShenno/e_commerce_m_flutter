/* --
      LIST OF Enums
      They cannot be created inside a class.
-- */

enum TextSize { small, medium, large }

enum OrderStatus { processing, shipped, delivered }

enum PaymentMethods { paypal, applePay, creditCard }
